{
  "options": {
    "config": ".jscsrc"
  },

  "reminder": {
    "src": ["<%= paths.scripts.all %>"],
    "options": {
      "force": true
    }
  },

  "write": {
    "src": ["<%= paths.scripts.all %>"],
    "options": {
      "fix": true
    }
  }
}
